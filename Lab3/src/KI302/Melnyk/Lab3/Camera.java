package KI302.Melnyk.Lab3;

/**
 * Interface Camera defines a contract for devices that can take photos.
 */
public interface Camera {
    /**
     * Method prototype for taking a photo.
     * Classes implementing this interface must provide a concrete implementation.
     */
    void takePhoto();
}